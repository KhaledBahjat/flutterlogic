import 'package:flutter/material.dart';

class ToggelSelection extends StatelessWidget {
  const ToggelSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.blueAccent,
    );
  }
}
