import 'package:flutter/material.dart';

class SingelSelcetion extends StatelessWidget {
  const SingelSelcetion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green.shade800,
    );
  }
}
